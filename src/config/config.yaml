apps:
  cab:
    category: cab
    validation:
      name_match_threshold: 75
      address_match_threshold: 40e
  meal:
    category: meal
    validation:
      vendor_match_threshold: 60
paths:
  clients_file: clients.json
validation:
  name_match_threshold: 75
  address_match_threshold: 40

llm:
  model: llama-3.3-70b-versatile
  temperature: 0